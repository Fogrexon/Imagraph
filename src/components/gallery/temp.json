[
  {
    "id": "id",
    "detail": {
      "title": "One",
      "shaders": {
        "default": {
          "name": "default",
          "shader": "void main() { gl_FragColor = texture2D(renderTexture, vUv); }"
        },
        "input": {
          "name": "default-input"
        }
      },
      "tree": {
        "type": "Filter",
        "id": "default",
        "input": [{
          "type": "Input",
          "id": "input"
        }]
      },
      "tags": ["shader"]
    }
  },
  {
    "id": "bbbbb",
    "detail": {
      "title": "One",
      "shaders": {
        "default": {
          "name": "default",
          "shader": "void main() { gl_FragColor = vec4(vec3(1.0) - texture2D(renderTexture, vUv).rgb, 1.0); }"
        },
        "input": {
          "name": "default-input"
        }
      },
      "tree": {
        "type": "Filter",
        "id": "default",
        "input": [{
          "type": "Input",
          "id": "input"
        }]
      },
      "tags": ["shader"]
    }
  },
  {
    "id": "ccccc",
    "detail": {
      "title": "One",
      "shaders": {
        "default": {
          "name": "default",
          "shader": "void main() { gl_FragColor = texture2D(renderTexture, vUv + vec2(sin(time), cos(time)) * 0.05); }"
        },
        "input": {
          "name": "default-input"
        }
      },
      "tree": {
        "type": "Filter",
        "id": "default",
        "input": [{
          "type": "Input",
          "id": "input"
        }]
      },
      "tags": ["shader"]
    }
  }
]